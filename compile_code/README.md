# Compile code for speed up, efficiency, better performance and protection of source code.

# Quick Start
To compile your Python code, follow these steps:
```bash
python compile.py build_ext --inplace
```

To clean up compiled files, run:
```bash
python compile.py clean
```